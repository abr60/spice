# Spice Configuration Architecture

Everything under `config/` is deployed to `~/.config/`. **Most dirs are stow
symlinks; `omarchy` is copied** (see `setup.md`). This guide covers the
Hyprland file chain, the Omarchy shell layout, installed plugins, themed
templates, hooks, the opencode config, and the `system/` directory.

## How Configs Reach the System

1. `install/config/stow.sh` wipes the target dirs (calibre, hypr, mpd,
   rmpc, sioyek, uwsm, yazi) and runs:
   ```bash
   stow --target="$HOME/.config" --ignore='^omarchy$' config
   ```
2. `install/config/omarchy.sh` then **copies** `branding`, `hooks`,
   `themed`, `shell.json` from `config/omarchy/` into `~/.config/omarchy/`
   (expanding the `__HOME__` path tokens in `shell.json` to `$HOME`).
   (Third-party plugins are installed separately at runtime by
   `install/extras/plugins.sh` — the `Plugins` setup step.)
3. Theme-derived symlinks (yazi theme, rmpc theme) are regenerated by the
   theme-set hooks.

Editing `~/.config/hypr/...` edits `config/hypr/...` (symlink), but editing
`~/.config/omarchy/...` does NOT touch the repo (copy) — edit the repo and
re-run `omarchy.sh`.

## Hyprland (`config/hypr/`)

`hyprland.lua` is the entry point. It bootstraps from Omarchy defaults, then
`require`s each personal file:

```lua
dofile((os.getenv("OMARCHY_PATH") or "/usr/share/omarchy") .. "/default/hypr/bootstrap.lua")
require("default.hypr.omarchy")
require("hypr.monitors")
require("hypr.input")
require("hypr.bindings")
require("hypr.looknfeel")
require("hypr.autostart")
require("hypr.rules")
require("default.hypr.toggles")
```

| File | Purpose |
|------|---------|
| `monitors.lua` | GDK_SCALE=1, monitor scale 1.25, `mode = "preferred"`, commented examples for per-monitor config + portrait rotation |
| `input.lua` | Input/keyboard settings |
| `bindings.lua` | Custom keybindings (below) |
| `looknfeel.lua` | Master layout, gaps 2, border 2, rounding 6, opacity, blur, Bibata cursor (24px) |
| `autostart.lua` | `o.exec_on_start("hyprsunset")` (battery-monitor was removed) |
| `rules.lua` | Window rules for Waydroid, Yazi, media-download, rmpc, wifi-qr, audacious + overview layer rules |
| `hyprsunset.conf` | Night light (hyprsunset) profiles |
| `xdph.conf` | XDG desktop portal config |
| `assets/` | Branding logos (`grabber.txt`, `push.txt`, `mp4-to-gif.txt`, ...) and helper lib used by bin/ scripts |

### Keybindings (`bindings.lua`)

| Binding | Action |
|---------|--------|
| `ALT + comma` | Unmount HDD (`hdd-unmount`) |
| `XF86NotificationCenter` | WhatsApp webapp |
| `XF86Favorites` | Calibre (`ebook-viewer`) |
| `ALT + C` | RMPC music (`[float; center] foot -e rmpc`) |
| `ALT + X` | Study folder in Yazi (30s timeout) |
| `SUPER + M` | Comic translate Latin (`comic-translate lat`) |
| `SUPER + N` | Comic translate CJK (`comic-translate cjk`) |
| `SUPER + Alt_L` | Toggle overview (`omarchy-shell shell toggle overview`) |
| `SUPER + A` | Launch default AI agent (`omarchy-agent` → opencode) |
| `ALT + Z` / `F9` | Voxtype dictation (only if `voxtype` command exists) |

Note: the overview binding unbinds the Omarchy default (`hl.unbind`) before
rebinding, and calls `toggle overview` (the **short** plugin id — a previous
bug used the long `omarchy-overview` id, which the shell silently ignored).

#### Keybinding Quick Reference (read this FIRST)

- **File:** `config/hypr/bindings.lua` (stowed live — edits apply instantly
  with `hyprctl reload`, NO shell restart needed).
- **Syntax:** `o.bind("MODS + KEY", "Description", "command")` — defined in
  `/usr/share/omarchy/default/hypr/helpers.lua` (line ~81). `command` may be a
  plain string, a `hl.dsp.*` dispatcher, or a table like
  `{ launch = "cmd", focus = "^class$" }` / `{ webapp = "https://..." }`.
  Unbind defaults first: `hl.unbind("MODS + KEY")`.
- **KEY uses XKB keysym names, NOT abbreviations:** left Ctrl is
  `Control_L`, left Alt is `Alt_L` — `CTRL_L` / `ALT_L` fail with
  "Unknown keysym". Verify with: `hyprctl reload` → `hyprctl configerrors` →
  `hyprctl binds -j` (check the binding appears).
- **Fn key is NOT bindable on ThinkPads:** Fn is firmware/EC-level and never
  reaches Hyprland as a keysym (verified: `KEY_FN` bit 216 is absent from the
  AT keyboard keymap). Never suggest binding it.

### Window Rules (`rules.lua`)

- Waydroid → fullscreen
- Yazi (foot, title `Yazi`) → float 700×200 centered
- media-download (foot) → float 900×400 centered
- rmpc-full (foot) → float 900×600 centered
- wifi-qr (foot) → float 336×416, pinned at 940×45
- audacious → float 500×300 at 13×515
- `overview` layer → blur + `ignore_alpha 0.2` (short namespace id)

## Omarchy Shell (`config/omarchy/shell.json`)

Bar layout (source of truth = `config/omarchy/shell.json`, pasted live by
`omarchy.sh`; plugins install to match it with no placement prompt):

| Section | Widgets (left→right) |
|---------|----------------------|
| **left** | omarchy.menu, yt-music (450×850 window), agx.screen-time, omaplug, io.github.boyoyooo.omarr (Sonarr), thinkfan, io.github.argusguardian.freemodels, io.github.elevate08.ynab-glance, io.github.silouanwright.look-elsewhere, omapets, io.github.thisisgm.cliampui |
| **center** | omarchy.indicators, omarchy.keyboard-layout, prettyletto.prettyzap, omarchy.clock (birth year 2004 / life expectancy 90; format `dddd h:mm AP`), omarchy.spacer, omarchy.system-update, omarchy.weather |
| **right** | omarchy.tray, jvb.omafocus, abran-labs.notification-center, maduki-tech.omado, io.github.grichard99.omaproton-vpn, andrewbacon.daynight, omarchy.tailscale, wian47.removable-drives, io.github.boyoyooo.omarr (Radarr), omarchy.agents, omarchy.dropbox, activity-monitor, abr.netshare, omarchy.audio, omarchy.bluetooth, omarchy.monitor, omarchy.power (no percentage) |

- Position: top, not transparent.
- Idle: screensaver 150s, lock 300s.
- Plugins section: `overview` (2 rows × 5 columns, hide empty rows, show
  special workspaces), `cursor-style`, `omapets-desktop`, `face.howdy`,
  `abr.lock`.
- Machine-specific paths (arr api-key files, theme wallpapers) are stored in
  the repo as `__HOME__` tokens and expanded to `$HOME` by `omarchy.sh` when
  pasted live.

### Restart the Shell After Major Changes (MANDATORY)

Hot-reload is NOT reliable for major shell/plugin changes. New plugin logic,
new profiles, new Process/daemon wiring, or stateful widget behavior can
silently fail to take effect while the shell keeps running — the log even
shows a clean reload while the widget still behaves as the old code. Real
example: the ThinkFan plugin's "Smart" profile showed as "balanced" and did
nothing until a full restart.

So after any major change to Omarchy shell/plugin code: **kill quickshell
and let Omarchy relaunch it.** Do NOT trust hot-reload for verification.

```bash
pgrep -a quickshell          # find instance, e.g. "667223 quickshell -n -p /usr/share/omarchy/shell"
kill <pid>                   # Omarchy auto-respawns (Hyprland autostart runs omarchy-launch-shell)
pgrep -a quickshell          # confirm new PID came up (must differ from killed one)
ls -td /run/user/1000/quickshell/by-id/*/ | head -1   # newest instance dir
tail -30 <dir>/log.log       # verify clean log, no errors for your plugin
```

If it does not come back: `omarchy restart shell` (or
`setsid omarchy-launch-shell`). Pre-existing unrelated warnings (e.g. missing
`opencode-zen.svg` in the agents plugin) are normal; only errors for YOUR
plugin/change matter.

## Plugins (installed at runtime)

Third-party shell plugins are **not vendored in the repo**. They are installed
on a fresh machine by `install/extras/plugins.sh` (the `Plugins` setup step),
which clones each upstream git repo, rewrites the manifest id to the **short
id** used in `config/omarchy/shell.json`, installs it into
`~/.config/omarchy/plugins/<id>`, and enables it without a placement prompt.

The script keeps an `id → repo URL` map (a `declare -A` array in
`install/extras/plugins.sh`). Enabled third-party ids referenced by
`shell.json` include: `yt-music`, `agx.screen-time`, `omaplug`,
`io.github.boyoyooo.omarr` (two bar entries, one install), `thinkfan`
(custom `abr60/thinkfan`), `io.github.argusguardian.freemodels`,
`io.github.elevate08.ynab-glance`,
`io.github.silouanwright.look-elsewhere`, `omapets`, `omapets-desktop`,
`io.github.thisisgm.cliampui`, `jvb.omafocus`,
`abran-labs.notification-center`, `maduki-tech.omado`,
`io.github.grichard99.omaproton-vpn`, `andrewbacon.daynight`,
`wian47.removable-drives`, `activity-monitor`, `abr.netshare`, `overview`,
`cursor-style`, `face.howdy`, `prettyletto.prettyzap`.

- First-party `omarchy.*` plugins ship with the system — never installed here.
- `abr.lock` is not a git repo: it is the stock `omarchy.lock` cloned/patched
  by `face.howdy`'s `omarchy-howdy-deploy-lock`, run as the last step of
  `plugins.sh`.
- `abr.network` (the old name) is **not** referenced — `abr.netshare` replaces it.
- New/organic plugins live under `~/.config/omarchy/plugins/` (Omarchy's
  territory at runtime); see [`plugins.md`](plugins.md) for our own heavily
  customized live-installed plugins (face.howdy, yt-music, omarr, netshare,
  thinkfan, abr.lock).

## Themed Templates (`config/omarchy/themed/`)

Templates processed by `omarchy-theme-set-templates` on every theme change,
plus hook-generated outputs:

| File | Purpose |
|------|---------|
| `yazi.toml.tpl` | yazi theme template; `{{ key }}` placeholders filled by `update-yazi-theme` hook |
| `omarchy.ron.tpl` | rmpc theme template; `{{ accent }}`, `{{ foreground }}`, `{{ muted }}`, `{{ dark_background }}`, `{{ lighter_background }}` → `omarchy.ron` |
| `alacritty.toml.tpl.sample` | sample template for alacritty |

## Hooks (`config/omarchy/hooks/`)

| Directory | Contents |
|-----------|----------|
| `battery-low.d/` | `persistent-nag` (battery low nag handler) |
| `theme-set.d/` | `update-yazi-theme`, `update-rmpc-theme` |

Notable hooks:

- **`theme-set.d/update-yazi-theme`** — regenerates
  `~/.config/yazi/theme.toml` from `yazi.toml.tpl`, substituting `{{ key }}`
  placeholders using `omarchy-theme-color --file <colors.toml> --all` (the
  **full resolved** color set, including derived aliases like
  `dark_background`, `lighter_background`, `light_foreground`), then symlinks
  it. A previous bug substituted only raw `colors.toml` keys, leaving `{{ }}`
  literals that crashed yazi.
- **`theme-set.d/update-rmpc-theme`** — symlinks the generated
  `omarchy.ron` from the current theme state dir into
  `~/.config/rmpc/themes/omarchy.ron`.

Theme state dir: `~/.local/state/omarchy/current/theme/`.

## opencode Config (`config/opencode/`)

Stowed to `~/.config/opencode/`. Contents:

| File | Purpose |
|------|---------|
| `opencode.json` | Main config: agents, models, plugin list |
| `tui.json` | TUI plugin overrides (DCP + quota) |
| `dcp.jsonc` | DCP (dynamic context pruning) — schema-only, defaults apply |
| `smart-title.jsonc` | Smart Title plugin settings (enabled, 5-turn context limit) |
| `plugin/vision-bridge.ts` | Custom plugin bridging vision subagent output |
| `package.json` / `package-lock.json` | Node deps for `vision-bridge.ts` (`@opencode-ai/plugin`) |

`opencode.json` current contents (key parts):

```json
{
  "$schema": "https://opencode.ai/config.json",
  "autoupdate": false,
  "default_agent": "build",
  "plugin": [
    "./plugin/vision-bridge.ts",
    "@tarquinen/opencode-dcp@latest",
    "opencode-smart-title",
    "opencode-simple-notify"
  ],
  "small_model": "opencode/nemotron-3.5-lightning-free",
  "provider": { "zen": { "type": "opencode-zen" } },
  "agent": {
    "plan":     { "mode": "primary",  "model": "opencode/big-pickle" },
    "build":    { "mode": "primary",  "model": "opencode/deepseek-v4-flash-free" },
    "quickfix": { "mode": "subagent", "model": "opencode/nemotron-3.5-lightning-free",
                  "permission": { "edit": "allow", "bash": "allow" } },
    "explore":  { "mode": "subagent", "model": "opencode/nemotron-3.5-lightning-free" },
    "vision":   { "mode": "subagent", "model": "opencode/mimo-v2.5-free" }
  }
}
```

Plugins installed: `vision-bridge.ts` (local, custom), DCP (context
pruning), smart-title, simple-notify.

> Changes require an opencode restart. Consult the `customize-opencode` skill
> before editing.

## System Configs (`system/`)

Not stowed; raw system-level configs kept in the repo:

- `easyeffects/presets/` — t14-dolby-game/movie/music/voice.json presets
- `thinkfan/` — `thinkfan.conf`, `fix-thinkfan-hwmon.sh`,
  `thinkfan-hwmon-fix.service` (ThinkPad fan curve + hwmon fix)

## Other Configs (`config/`)

- `calibre/` — Calibre settings
- `mpd/`, `rmpc/` — music player configs (mpd-mpris drop-in override
  is written by `install/services/mpd-rmpc.sh`)
  (mpv config moved out of spice on 2026-08-29 → own repo `abr60/mpv-config`,
  managed straight from `~/.config/mpv` as a git worktree. Fresh installs get
  it via `install/extras/mpv.sh` during setup — see `setup.md`/`scripts.md`)
- `sioyek/` — PDF reader config
- `uwsm/` — universal Wayland session manager config
- `yazi/` — file manager config (theme generated by the theme-set hook)
- `miscellaneous/` — assorted configs

## Applications (`applications/`)

Desktop entries and icons shipped by spice:

- `cliamp.desktop` — desktop entry for the CLI audio player
- `icons/` — bundled icons

## Common Tasks

- **Add a keybinding:** edit `config/hypr/bindings.lua`, then `hyprctl reload`
  — but remember the repo file is the source of truth.
- **Change bar layout:** edit `config/omarchy/shell.json`, then re-run
  `install/config/omarchy.sh` (or let the shell hot-reload) to push it live.
- **Add a plugin:** add an `id → repo URL` entry to
  `install/extras/plugins.sh` (the `declare -A PLUGIN_REPOS` map) and reference
  the id in `config/omarchy/shell.json`; `plugins.sh` fetches+installs it on a
  fresh machine. Install alive on this box with
  `omarchy plugin add <url> --enable --yes`.
- **Add a hook:** drop an executable in the right `.d` directory under
  `config/omarchy/hooks/`.
- **Add a themed template:** put a `.tpl` in `config/omarchy/themed/`.
- **Tweak look & feel:** `config/hypr/looknfeel.lua` (gaps, blur, rounding,
  cursor).