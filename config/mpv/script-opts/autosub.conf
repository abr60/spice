# Auto-download subtitles when a video opens.
# NOTE: This config is NOT read by scripts/autosub.lua — that script has its
# languages hardcoded (English + Bangla) and logs in to OpenSubtitles
# anonymously (the logins array is commented out). The old credentials were
# removed: they were unused AND committed to the repo.
auto=yes
languages=en,bn